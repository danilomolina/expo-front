export type EventModel = {
  id? : string
  title : string,
  caption: string,
  date: string,
  hour: number,
  local: string,
  observation: string,
  link: string,
  mentors: string,
  category: string
}
